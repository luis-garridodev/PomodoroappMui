import { createTheme } from "@mui/material"
import { red,orange } from "@mui/material/colors"

export const ClockPause=createTheme({
    palette:{
        primary:{
            main:red[500]
        },
        secondary:{
            main:orange[900]
        }


    }
})

